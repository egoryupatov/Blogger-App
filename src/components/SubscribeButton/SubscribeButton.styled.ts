import styled from "styled-components";

export const SubscribeButtonStyled = styled.button`
  font-weight: 500;
  background: #f0f0f0;
  color: #000;
  font-size: 13px;
  padding: 0 12px;
  height: 26px;
  border-radius: 7px;
  border: none;

  &:hover {
    background: #e6e6e6;
  }

  &:active {
    background: #dbdbdb;
  }
`;
